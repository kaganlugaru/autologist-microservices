# 🚀 ПЛАН РАЗМЕЩЕНИЯ КОМПОНЕНТОВ AUTOLOGIST

## 📋 Финальная архитектура размещения

### 🎯 **Компоненты и их размещение:**

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   🌐 FRONTEND   │    │  ⚙️ BACKEND API  │    │  📱 TELEGRAM    │
│     Vercel      │◄──►│   Railway Web   │◄──►│ PARSER (Cron)   │
│   (Бесплатно)   │    │   Service 24/7  │    │   Railway Cron  │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         ▲                       ▲                       ▲
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  👤 ПОЛЬЗОВАТЕЛЬ │    │  🗄️ SUPABASE    │    │ 🤖 ИИ СЕРВИСЫ   │
│   (Браузер)     │    │   PostgreSQL    │    │ DeepSeek/Gemini │
│                 │    │   (Бесплатно)   │    │   (API calls)   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 💰 **Стоимость в месяц:**
- **Frontend (Vercel)**: $0 ✅
- **Backend API (Railway)**: $5 ✅  
- **Telegram Parser (Railway Cron)**: Включен в $5 ✅
- **База данных (Supabase)**: $0 ✅
- **ИИ обработка**: ~$2-5 📊
- **Итого**: ~$7-10/мес 💸

## 🔧 **Настройка Railway для Backend:**

### Backend как Web Service на Railway:
```javascript
// package.json
{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": "18.x"
  }
}

// server.js
const PORT = process.env.PORT || 3001;
app.listen(PORT, '0.0.0.0', () => {
  console.log(`🚀 Backend запущен на порту ${PORT}`);
});
```

### Telegram Parser как Cron Job на Railway:
```python
# requirements.txt для Cron Job
telethon==1.24.0
psycopg2-binary==2.9.3
python-decouple==3.6

# cron.py - основной файл парсера
import asyncio
from telethon import TelegramClient

async def parse_messages():
    # Логика парсинга
    pass

if __name__ == "__main__":
    asyncio.run(parse_messages())
```

## 📦 **Переменные окружения для Railway:**

### Backend Web Service:
```env
NODE_ENV=production
PORT=$PORT  # Railway автоматически
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_key
```

### Telegram Parser Cron:
```env
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
TELEGRAM_PHONE=your_phone
DATABASE_URL=your_supabase_connection_string
SCHEDULE="*/10 * * * *"  # каждые 10 минут
```

## 🚀 **Этапы деплоя:**

### 1️⃣ **Backend на Railway (Web Service):**
```bash
# Подключаем GitHub репозиторий
# Railway автоматически определит Node.js проект
# Настраиваем переменные окружения
# Деплой происходит автоматически при push
```

### 2️⃣ **Telegram Parser на Railway (Cron Job):**
```bash
# Создаем отдельный сервис в том же проекте
# Выбираем Cron Job
# Указываем команду: python cron.py
# Настраиваем расписание: */10 * * * *
```

### 3️⃣ **Frontend на Vercel:**
```bash
# Подключаем GitHub репозиторий
# Vercel автоматически определит React проект
# Настраиваем переменные окружения для API
# Деплой автоматический при push в main
```

## 🔄 **Преимущества такой архитектуры:**

✅ **Надежность**: Компоненты работают независимо
✅ **Масштабируемость**: Можно добавить больше парсеров  
✅ **Экономность**: Оптимальное использование бесплатных лимитов
✅ **Простота**: Автоматические деплои через Git
✅ **Мониторинг**: Railway и Vercel предоставляют логи

## 🎯 **Рекомендация:**

**Используем Railway для Backend Web Service!** 

Причины:
1. **24/7 работа** без sleep mode
2. **$5/мес покрывает** и backend, и cron job
3. **Простая настройка** через GitHub
4. **Автоматические перезапуски** при сбоях
5. **Соответствует** техническому заданию

---
🚀 **Готовы к деплою?** Начинаем с настройки Railway проекта!