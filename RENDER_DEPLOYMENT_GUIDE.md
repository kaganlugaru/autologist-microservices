# 🚀 ДЕПЛОЙ BACKEND НА RENDER.COM

## 📋 Пошаговая инструкция

### 1️⃣ **Подготовка GitHub репозитория**

Убедитесь, что все изменения зафиксированы и загружены:

```bash
# Добавляем все файлы
git add .

# Коммитим изменения
git commit -m "🚀 Подготовка backend для Render.com deployment

✨ Добавлено:
- Health check endpoint /api/health
- Root endpoint с информацией о API
- Настройка для работы на 0.0.0.0
- render.yaml конфигурация
- Подробные логи запуска"

# Пушим в GitHub
git push origin main
```

### 2️⃣ **Регистрация на Render.com**

1. Переходим на https://render.com
2. Нажимаем **"Get Started for Free"**
3. Регистрируемся через GitHub аккаунт
4. Подтверждаем доступ к репозиториям

### 3️⃣ **Создание Web Service**

1. На дашборде Render нажимаем **"New +"**
2. Выбираем **"Web Service"**
3. Подключаем GitHub репозиторий `autologist-microservices`
4. Настраиваем параметры:

```
Name: autologist-backend
Environment: Node
Region: Oregon (US West) - ближе к нам
Branch: main
Build Command: cd backend && npm install
Start Command: cd backend && npm start
```

### 4️⃣ **Настройка переменных окружения**

В разделе **Environment Variables** добавляем:

```env
NODE_ENV=production
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
```

**🔑 Где взять Supabase ключи:**
1. Заходим в Supabase Dashboard
2. Выбираем проект → Settings → API
3. Копируем:
   - Project URL → `SUPABASE_URL`
   - `anon` `public` → `SUPABASE_ANON_KEY`  
   - `service_role` `secret` → `SUPABASE_SERVICE_ROLE_KEY`

### 5️⃣ **Настройка Health Check**

В разделе **Health Check Path** указываем:
```
/api/health
```

### 6️⃣ **Запуск деплоя**

1. Нажимаем **"Create Web Service"**
2. Render автоматически:
   - Клонирует репозиторий
   - Установит зависимости  
   - Запустит сервер
   - Назначит URL

### 7️⃣ **Проверка деплоя**

После успешного деплоя:

1. **Проверяем URL сервиса** (вида `https://autologist-backend-xxx.onrender.com`)
2. **Тестируем endpoints:**

```bash
# Health check
curl https://your-service-url.onrender.com/api/health

# Root endpoint  
curl https://your-service-url.onrender.com/

# API status
curl https://your-service-url.onrender.com/api/status
```

### 8️⃣ **Настройка UptimeRobot (решение Sleep Mode)**

1. Регистрируемся на https://uptimerobot.com (бесплатно)
2. Создаем новый монитор:
   - **Monitor Type**: HTTP(s)
   - **URL**: `https://your-service-url.onrender.com/api/health`
   - **Monitoring Interval**: 5 minutes
   - **Monitor Timeout**: 30 seconds

3. Это будет пинговать ваш сервис каждые 5 минут, предотвращая sleep mode

## ✅ **Результат**

После выполнения всех шагов у вас будет:

- ✅ **Backend работает 24/7** на Render.com
- ✅ **Автоматические деплои** при push в GitHub
- ✅ **SSL сертификат** включен автоматически
- ✅ **Логи и мониторинг** в дашборде Render
- ✅ **Health check** для контроля состояния
- ✅ **UptimeRobot** предотвращает sleep mode

## 🔧 **Полезные команды**

### Локальная проверка:
```bash
# Запуск локально
cd backend
npm start

# Тест health check
curl http://localhost:3001/api/health
```

### Проверка на Render:
```bash
# Проверка статуса API
curl https://your-service-url.onrender.com/api/status

# Проверка переменных окружения (в логах Render)
# Должно показать: "✅ База данных: подключена"
```

## 🚨 **Устранение проблем**

### Если деплой не удался:
1. Проверьте логи в Render Dashboard
2. Убедитесь, что переменные окружения корректны
3. Проверьте, что Supabase доступен

### Если сервис засыпает:
1. Убедитесь, что UptimeRobot настроен
2. Проверьте, что health check отвечает

### Если база данных недоступна:
1. Проверьте Supabase ключи
2. Убедитесь, что проект Supabase активен

---
🎯 **После успешного деплоя переходим к настройке Frontend на Vercel!**