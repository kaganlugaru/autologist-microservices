# 🛠️ Создание Web Service для бэкенда на Render.com

## Проблема
Ваш текущий сервис имеет тип "Background Worker" - у него нет публичного URL для API запросов.

## Решение
Создать новый **Web Service** для бэкенда с публичным URL.

---

## 📋 Пошаговая инструкция

### Шаг 1: Создание нового Web Service

1. **Откройте https://dashboard.render.com**
2. **Нажмите синюю кнопку "New" в правом верхнем углу**
3. **Выберите "Web Service"** из выпадающего меню

### Шаг 2: Подключение репозитория

1. **Выберите "Build and deploy from a Git repository"**
2. **Найдите репозиторий** `kaganlugaru/autologist-microservices`
3. **Нажмите "Connect"**

### Шаг 3: Конфигурация сервиса

Заполните следующие поля:

**Основные настройки:**
- **Name:** `autologist-backend-api`
- **Region:** `Frankfurt (EU Central)` (или любой ближайший)
- **Branch:** `main`
- **Root Directory:** `backend`

**Runtime настройки:**
- **Runtime:** `Node`
- **Build Command:** `npm install`
- **Start Command:** `npm start`

**План:**
- **Instance Type:** `Free` (0.5 CPU, 512 MB RAM)

### Шаг 4: Переменные окружения

Добавьте следующие переменные в разделе "Environment Variables":

```
SUPABASE_URL=https://sfjxwagatlcyhuellwlc.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InNmanh3YWdhdGxjeWh1ZWxsd2xjIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2MjIwNTc1MSwiZXhwIjoyMDc3NzgxNzUxfQ.mT1jJtepNY8m8LSek7aSsEHDtmys2ur7zl9iv31B3es
NODE_ENV=production
```

### Шаг 5: Создание сервиса

1. **Нажмите "Create Web Service"**
2. **Ждите завершения первого деплоя** (5-10 минут)

### Шаг 6: Получение URL

После успешного деплоя:
1. **Скопируйте URL** из верхней части страницы сервиса
2. **URL будет примерно:** `https://autologist-backend-api.onrender.com`

### Шаг 7: Проверка работы

Откройте в браузере:
```
https://ваш-новый-url.onrender.com/api/health
```

Должен вернуться ответ:
```json
{
  "status": "OK",
  "timestamp": "2025-11-06T...",
  "uptime": "...",
  "database": "connected"
}
```

---

## ✅ Что дальше

1. **После получения URL** - обновите переменную `VITE_API_BASE_URL` для фронтенда
2. **Разверните фронтенд** на Vercel с новым URL
3. **Удалите старый Background Worker** (если не нужен)

---

## 🚨 Частые ошибки

**Ошибка:** "Build failed"
- **Решение:** Убедитесь, что Root Directory = `backend`

**Ошибка:** "Start command failed"  
- **Решение:** Проверьте переменные окружения

**Ошибка:** 503 Service Unavailable
- **Решение:** Подождите завершения деплоя

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в разделе "Logs"
2. Убедитесь, что все переменные окружения добавлены
3. Проверьте правильность Root Directory