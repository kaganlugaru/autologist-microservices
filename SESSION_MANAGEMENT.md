# 🔧 СИСТЕМА УПРАВЛЕНИЯ TELEGRAM СЕССИЯМИ

## 📋 Описание проблемы
Изначально у нас был конфликт IP-адресов: одна и та же Telegram сессия использовалась одновременно на Railway (продакшн) и локально (разработка). Telegram блокирует такое использование.

## ✅ Решение: Разделение сессий

### 🏗️ Структура сессий
```
📁 autologist-microservices/
├── railway_production.session   # 180 KB - для Railway продакшн
├── local_development.session    # 28 KB  - для локальной разработки
└── sessions_backup/             # Бэкап всех старых сессий
```

### ⚙️ Система автовыбора
**В `telegram_parser.py`:**
```python
# Выбираем сессию в зависимости от окружения
if os.getenv('RAILWAY_ENVIRONMENT') == 'production':
    self.session_name = 'railway_production'
    logger.info("🚄 Используем Railway production сессию")
else:
    self.session_name = 'local_development'  
    logger.info("💻 Используем локальную development сессию")
```

### 🔄 Управление окружениями

#### 🛠️ Через Python менеджер:
```bash
python session_manager.py
```

#### 🛠️ Через batch скрипты:
- `switch_session.bat` - интерактивное переключение
- `set_railway_production.bat` - быстрое переключение на продакшн

## 📊 Текущее состояние

### ✅ Что работает:
- ✅ Система выбора сессий по окружению
- ✅ Автоматическое переключение локально/Railway  
- ✅ Бэкап всех старых сессий
- ✅ Удобные инструменты управления

### ⚠️ Ожидает решения:
- ⏳ **Railway должен перезапуститься** с новой переменной `RAILWAY_ENVIRONMENT=production`
- ⏳ После перезапуска Railway будет использовать `railway_production.session`
- ⏳ Тогда локально сможем использовать `local_development.session` без конфликтов

## 🚀 Инструкция по развертыванию

### 1️⃣ Railway (продакшн):
```bash
# В настройках Railway добавить:
RAILWAY_ENVIRONMENT=production

# Сессия будет: railway_production.session (180 KB)
```

### 2️⃣ Локально (разработка):
```bash
# В .env файле:
RAILWAY_ENVIRONMENT=development

# Сессия будет: local_development.session (28 KB)
```

## 🔬 Диагностика

### Проверить текущее окружение:
```bash
python session_manager.py
```

### Посмотреть логи парсера:
```bash
cd telegram-parser
python telegram_parser.py
# В логах будет: "💻 Используем локальную development сессию"
# или: "🚄 Используем Railway production сессию"
```

## 🚨 Устранение проблем

### Ошибка "authorization key used under two different IP addresses":
**Причина:** Один номер телефона используется с двух IP-адресов  
**Решение:** Подождать 10-15 минут пока Railway переключится на новую сессию

### Сессия не найдена:
```bash
# Проверить наличие файлов:
dir *.session

# Ожидаемый результат:
# local_development.session
# railway_production.session
```

## 📂 Файлы системы
- `session_manager.py` - Python менеджер сессий
- `switch_session.bat` - Batch переключатель
- `set_railway_production.bat` - Быстрая настройка Railway
- `sessions_backup/` - Бэкап всех старых сессий

## 🎯 Следующие шаги
1. ⏳ Дождаться перезапуска Railway с `RAILWAY_ENVIRONMENT=production`
2. ✅ Протестировать локальный запуск без конфликтов
3. ✅ Убедиться что Railway использует правильную сессию
4. 🗑️ Очистить старые сессии из бэкапа (опционально)