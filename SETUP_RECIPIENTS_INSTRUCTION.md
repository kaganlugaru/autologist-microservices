# 📋 Инструкция по настройке системы отправки сообщений

## ✅ Что уже сделано

### 1. Улучшенные логи парсера
- ✅ Добавлено детальное логирование работы с дубликатами
- ✅ Улучшен формат сообщений в логах
- ✅ Парсер показывает подробную информацию о проверке дубликатов

### 2. Функция отправки сообщений получателям
- ✅ Добавлена функция `send_message_to_recipients()` в парсер
- ✅ Парсер автоматически отправляет найденные сообщения получателям
- ✅ Система поддерживает множественных получателей для одного ключевого слова

### 3. Интерфейс управления получателями
- ✅ Создан компонент `RecipientsManager` во фронтенде
- ✅ Добавлены API endpoints в backend для работы с получателями
- ✅ Интерфейс позволяет добавлять/удалять/активировать получателей

## 🔧 Что нужно сделать

### 1. Создать таблицу в Supabase (ОБЯЗАТЕЛЬНО!)

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в SQL Editor
4. Выполните SQL из файла `create_recipients_table.sql`:

```sql
-- Создание таблицы для получателей сообщений по ключевым словам
CREATE TABLE message_recipients (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    username VARCHAR(255),
    telegram_id BIGINT NOT NULL,
    keyword VARCHAR(255) NOT NULL,
    active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Создаем индексы для быстрого поиска
CREATE INDEX idx_recipients_keyword ON message_recipients(keyword);
CREATE INDEX idx_recipients_telegram_id ON message_recipients(telegram_id);
CREATE INDEX idx_recipients_active ON message_recipients(active);

-- Создаем уникальный индекс для предотвращения дубликатов
CREATE UNIQUE INDEX unique_recipient_keyword ON message_recipients(telegram_id, keyword);

-- Добавляем получателя KGN для всех ключевых слов
INSERT INTO message_recipients (name, username, telegram_id, keyword, active) VALUES
('KGN', 'Rinat575kz', 262700292, 'доставка', true),
('KGN', 'Rinat575kz', 262700292, 'перевозка', true),
('KGN', 'Rinat575kz', 262700292, 'Китай', true),
('KGN', 'Rinat575kz', 262700292, 'Москва', true),
('KGN', 'Rinat575kz', 262700292, 'горячий', true),
('KGN', 'Rinat575kz', 262700292, 'стоимость', true);

-- Включаем Row Level Security (RLS) для безопасности
ALTER TABLE message_recipients ENABLE ROW LEVEL SECURITY;

-- Создаем политику для полного доступа (можно настроить позже)
CREATE POLICY "Enable all operations for message_recipients" ON message_recipients
FOR ALL USING (true) WITH CHECK (true);
```

### 2. Проверить работу системы

1. **Запустите frontend:**
   ```bash
   cd frontend
   npm run dev
   ```

2. **Откройте вкладку "📤 Получатели"** - теперь там должен отображаться интерфейс управления

3. **Отправьте тестовое сообщение:**
   - Зайдите в чат "тест автологист" 
   - Напишите сообщение с ключевым словом, например: "нужна доставка из Китая"
   - Парсер должен обнаружить ключевое слово и отправить уведомление KGN

## 📊 Как работает система

### Алгоритм обработки:
1. **Парсер получает новое сообщение** из отслеживаемого чата
2. **Проверяет на дубликаты** - подробные логи покажут процесс проверки
3. **Ищет ключевые слова** в тексте сообщения
4. **Если найдены ключевые слова:**
   - Сохраняет сообщение в БД
   - Получает список получателей для этих ключевых слов из таблицы `message_recipients`
   - Отправляет уведомление каждому получателю через Telegram

### Пример уведомления:
```
НОВОЕ СООБЩЕНИЕ ПО КЛЮЧЕВЫМ СЛОВАМ

Чат: тест автологист
Ключевые слова: доставка, Китай

Текст сообщения:
нужна доставка из Китая

---
Время: 2025-11-04 23:30:15
```

## 🎯 Логи дубликатов

Теперь парсер выводит подробные логи при обработке дубликатов:
```
ДУБЛИКАТ: Обнаружено похожее сообщение
ДУБЛИКАТ: Хеш контента: a1b2c3d4e5f6...
ДУБЛИКАТ: Пользователь ID: 123456789
ДУБЛИКАТ: Возраст существующего: 2.5 часов
ДУБЛИКАТ: Оригинальное сообщение: старый текст сообщения...
ДУБЛИКАТ: Новое сообщение: новый текст сообщения...
ДУБЛИКАТ: РЕШЕНИЕ - сообщение свежее, отклоняем как ДУБЛИКАТ
```

## 🔍 Отладка

### Если сообщения не отправляются:
1. Проверьте, что таблица `message_recipients` создана
2. Убедитесь, что получатели добавлены для нужных ключевых слов
3. Проверьте логи парсера на наличие ошибок
4. Убедитесь, что Telegram ID получателя корректный

### Проверка логов:
```bash
cd telegram-parser
tail -f logs/telegram_parser.log
```

## 📱 Управление получателями

Во фронтенде на вкладке "📤 Получатели" вы можете:
- ➕ Добавлять новых получателей
- 🔴/🟢 Активировать/деактивировать получателей
- 🗑️ Удалять получателей
- 👀 Просматривать всех получателей по группам

Каждый получатель привязан к конкретному ключевому слову - это позволяет настроить разных получателей для разных типов сообщений.