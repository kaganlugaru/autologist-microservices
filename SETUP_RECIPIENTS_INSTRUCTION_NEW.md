# 📋 Инструкция по настройке системы отправки сообщений

## ✅ Что уже сделано

### 1. Улучшенные логи парсера
- ✅ Добавлено детальное логирование работы с дубликатами
- ✅ Улучшен формат сообщений в логах
- ✅ Парсер показывает подробную информацию о проверке дубликатов

### 2. Функция отправки сообщений получателям
- ✅ Добавлена функция `send_message_to_recipients()` в парсер
- ✅ Парсер автоматически отправляет найденные сообщения получателям **по username**
- ✅ Система поддерживает множественных получателей для одного ключевого слова

### 3. Интерфейс управления получателями
- ✅ Добавлена секция управления получателями во вкладку "⚙️ Управление"
- ✅ Добавлены API endpoints в backend для работы с получателями
- ✅ **Упрощенный интерфейс**: добавление получателей только по username (без telegram ID)
- ✅ Автоматический выбор из существующих ключевых слов

## 🔧 Что нужно сделать

### 1. Создать таблицу в Supabase (ОБЯЗАТЕЛЬНО!)

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в SQL Editor
4. Выполните SQL из файла `create_recipients_table.sql`:

```sql
-- Создание таблицы для получателей сообщений по ключевым словам
CREATE TABLE message_recipients (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    username VARCHAR(255) NOT NULL,
    telegram_id BIGINT DEFAULT NULL,
    keyword VARCHAR(255) NOT NULL,
    active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Создаем индексы для быстрого поиска
CREATE INDEX idx_recipients_keyword ON message_recipients(keyword);
CREATE INDEX idx_recipients_username ON message_recipients(username);
CREATE INDEX idx_recipients_active ON message_recipients(active);

-- Создаем уникальный индекс для предотвращения дубликатов
CREATE UNIQUE INDEX unique_recipient_keyword ON message_recipients(username, keyword);

-- Добавляем получателя KGN для всех ключевых слов
INSERT INTO message_recipients (name, username, keyword, active) VALUES
('KGN', 'Rinat575kz', 'доставка', true),
('KGN', 'Rinat575kz', 'перевозка', true),
('KGN', 'Rinat575kz', 'Китай', true),
('KGN', 'Rinat575kz', 'Москва', true),
('KGN', 'Rinat575kz', 'горячий', true),
('KGN', 'Rinat575kz', 'стоимость', true);

-- Включаем Row Level Security (RLS) для безопасности
ALTER TABLE message_recipients ENABLE ROW LEVEL SECURITY;

-- Создаем политику для полного доступа (можно настроить позже)
CREATE POLICY "Enable all operations for message_recipients" ON message_recipients
FOR ALL USING (true) WITH CHECK (true);
```

### 2. Проверить работу системы

1. **Откройте вкладку "⚙️ Управление"** - теперь там есть секция "📤 Управление получателями уведомлений"

2. **Добавьте получателя:**
   - Имя: KGN
   - Username: Rinat575kz (без @)
   - Ключевое слово: выберите из списка

3. **Отправьте тестовое сообщение:**
   - Зайдите в чат "тест автологист" 
   - Напишите сообщение с ключевым словом, например: "нужна доставка из Китая"
   - Парсер должен обнаружить ключевое слово и отправить уведомление @Rinat575kz

## 📊 Как работает система

### Алгоритм обработки:
1. **Парсер получает новое сообщение** из отслеживаемого чата
2. **Проверяет на дубликаты** - подробные логи покажут процесс проверки
3. **Ищет ключевые слова** в тексте сообщения
4. **Если найдены ключевые слова:**
   - Сохраняет сообщение в БД
   - Получает список получателей для этих ключевых слов из таблицы `message_recipients`
   - **Отправляет уведомление каждому получателю по @username через Telegram**

### Пример уведомления:
```
НОВОЕ СООБЩЕНИЕ ПО КЛЮЧЕВЫМ СЛОВАМ

Чат: тест автологист
Ключевые слова: доставка, Китай

Текст сообщения:
нужна доставка из Китая

---
Время: 2025-11-04 23:30:15
```

## 🎯 Преимущества нового подхода

### ✅ Упрощенное управление:
- **Добавление по username** - не нужно узнавать Telegram ID
- **Встроенный интерфейс** - всё в одной вкладке "Управление"
- **Выбор из существующих ключевых слов** - нет ошибок в написании

### ✅ Гибкость:
- Один пользователь может получать уведомления по разным ключевым словам
- Легко добавлять/удалять ключевые слова для конкретного пользователя
- Возможность временно отключать уведомления (кнопка 🔴/🟢)

## 🔍 Отладка

### Если сообщения не отправляются:
1. ✅ Проверьте, что таблица `message_recipients` создана
2. ✅ Убедитесь, что получатель добавлен для нужного ключевого слова
3. ✅ Проверьте логи парсера на наличие ошибок отправки
4. ✅ Убедитесь, что username получателя написан правильно (без @)

### Проверка логов:
```bash
cd telegram-parser
tail -f logs/telegram_parser.log
```

Ищите строки типа:
```
ОТПРАВКА: Сообщение отправлено KGN (@Rinat575kz)
```

## 📱 Управление получателями

Во вкладке "⚙️ Управление" в секции "📤 Управление получателями уведомлений" вы можете:
- ➕ **Добавлять получателей** - просто укажите имя, username и выберите ключевое слово
- 🔴/🟢 **Активировать/деактивировать** уведомления для конкретного ключевого слова
- 🗑️ **Удалять** ненужные привязки получатель-ключевое слово
- 👀 **Просматривать** всех получателей, сгруппированных по пользователям

### Примеры использования:
- **KGN получает все уведомления:** добавьте @Rinat575kz для каждого ключевого слова
- **Разные получатели для разных тем:** добавьте разных людей для "доставка" и "стоимость"
- **Временное отключение:** используйте кнопку 🔴 вместо удаления