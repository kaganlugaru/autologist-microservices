# 📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Система автоматизации грузоперевозок (ОБНОВЛЕНО 06.11.2025)

## 🎯 ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА
**Цель:**
Автоматизация сбора и обработки заказов из чатов мессенджеров для логиста/экспедитора с минимальными затратами.

**Ключевые функции:**
- 📱 Парсинг 20+ чатов Telegram и WhatsApp (~6000 сообщений/день)
- 🧠 Умная обработка ИИ с дедупликацией и приоритетом по цене
- 🤖 Авто-ответы и ведение переговоров
- 📊 Статистика и аналитика в реальном времени
- 🌐 Веб-интерфейс с мобильной версией

---

## 🏗️ АРХИТЕКТУРА СИСТЕМЫ (ТЕКУЩЕЕ СОСТОЯНИЕ)

### ✅ РАЗВЕРНУТЫЕ КОМПОНЕНТЫ:
1. **✅ Парсер Telegram** (Python, Telethon, Railway) - **РАБОТАЕТ**
   - Мониторинг 7 чатов в реальном времени
   - Поиск по 3 ключевым словам: '17м', 'тандем', 'груз'
   - Автоматическое сохранение в Supabase
   
2. **✅ База данных** (Supabase/PostgreSQL) - **РАБОТАЕТ**
   - Таблицы: messages, keywords, monitored_chats
   - API для всех операций CRUD
   
3. **✅ Backend API** (Node.js/Express, Render.com) - **РАБОТАЕТ** 
   - REST API для всех операций
   - Управление ключевыми словами (GET/POST/PUT/DELETE)
   - Статистика и мониторинг
   
4. **✅ Веб-интерфейс** (React + Vite, Vercel) - **РАБОТАЕТ**
   - Просмотр сообщений в реальном времени
   - Статистика и аналитика
   - Управление Telegram чатами

### ⏳ КОМПОНЕНТЫ В РАЗРАБОТКЕ:
5. **🔨 Парсер WhatsApp** (Node.js, whatsapp-web.js) - **НЕ НАЧАТО**
6. **🔨 ИИ-обработчик** (DeepSeek, Hugging Face) - **НЕ НАЧАТО**
7. **🔨 Авто-отвечатель** (Node.js) - **НЕ НАЧАТО**
8. **🔨 Сервис рассылки объявлений** - **НЕ НАЧАТО**

---

## 🚨 ТЕКУЩИЕ ЗАДАЧИ И НЕДОСТАТКИ

### 🔧 СРОЧНЫЕ ИСПРАВЛЕНИЯ FRONTEND:

#### 1. **❌ ОТСУТСТВУЕТ: Управление ключевыми словами**
**Проблема:** В веб-интерфейсе нет возможности добавлять/удалять ключевые слова
**Статус:** Backend API готов, нужен UI компонент
**Решение:** Создать компонент KeywordsManager.jsx

#### 2. **❌ ОТСУТСТВУЕТ: Управление получателями уведомлений**
**Проблема:** Нет интерфейса для настройки Telegram уведомлений
**Статус:** Частично реализовано в RecipientsManager.jsx
**Решение:** Интегрировать в главный интерфейс

#### 3. **❌ ОГРАНИЧЕНО: Фильтрация и поиск сообщений**
**Проблема:** Нет продвинутой фильтрации по чатам, ключевым словам, датам
**Статус:** Базовая функциональность есть
**Решение:** Расширить MessageList.jsx

#### 4. **❌ ОТСУТСТВУЕТ: Bulk операции**
**Проблема:** Нельзя массово управлять сообщениями/чатами
**Статус:** Не реализовано
**Решение:** Добавить чекбоксы и массовые действия

### 🔧 BACKEND УЛУЧШЕНИЯ:

#### 5. **❌ ОТСУТСТВУЕТ: Пагинация для больших объемов**
**Проблема:** При большом количестве сообщений фронтенд тормозит
**Статус:** Limit 1000 сообщений
**Решение:** Добавить offset/page параметры

#### 6. **❌ ОТСУТСТВУЕТ: Продвинутая статистика**
**Проблема:** Базовые счетчики, нет детальной аналитики
**Статус:** Простые stats
**Решение:** Добавить временные интервалы, топ чатов, динамику

### 🔧 TELEGRAM PARSER УЛУЧШЕНИЯ:

#### 7. **❌ ОТСУТСТВУЕТ: Уведомления оператору**
**Проблема:** Парсер не отправляет найденные сообщения на Telegram
**Статус:** Логика есть, нет реализации
**Решение:** Добавить Telegram Bot API

#### 8. **❌ ОГРАНИЧЕНО: Дедупликация**
**Проблема:** Простая дедупликация, нет учета цен и вариантов
**Статус:** Базовая проверка
**Решение:** Реализовать по ТЗ (95% схожесть, цены, пользователи)

---

## 🎯 ПЛАН ДОРАБОТКИ (СЛЕДУЮЩИЕ 2 НЕДЕЛИ)

### НЕДЕЛЯ 1: Улучшение Frontend
**Приоритет 1 - Управление ключевыми словами:**
- Создать KeywordsManager.jsx компонент
- Добавить формы добавления/удаления ключевых слов
- Интегрировать в основной интерфейс (новая вкладка)

**Приоритет 2 - Улучшение фильтрации:**
- Расширить MessageList.jsx фильтрами
- Добавить поиск по тексту сообщений
- Фильтры по чатам, датам, ключевым словам

**Приоритет 3 - Массовые операции:**
- Чекбоксы для выбора сообщений
- Действия: удалить, пометить, экспорт

### НЕДЕЛЯ 2: Backend и парсер
**Приоритет 1 - Telegram уведомления:**
- Настроить Telegram Bot для уведомлений
- Реализовать отправку найденных сообщений оператору
- Добавить управление получателями

**Приоритет 2 - Улучшенная дедупликация:**
- Реализовать алгоритм по ТЗ (SequenceMatcher)
- Учет цен и разных пользователей
- Приоритет по цене

**Приоритет 3 - Пагинация и производительность:**
- Backend: добавить offset/limit
- Frontend: бесконечная прокрутка или страницы
- Оптимизация запросов

---

## 🔄 ЛОГИКА ДЕДУПЛИКАЦИИ, ФИЛЬТРАЦИИ И УВЕДОМЛЕНИЙ В ПАРСЕРЕ

**Дедупликация:**
- Сообщение считается дубликатом, если:
  - Оно от того же пользователя (user_id).
  - Текст совпадает на 95% (SequenceMatcher/Levenshtein).
  - Если цена изменилась — это новое объявление.
- Если сообщение от другого пользователя — это альтернатива, сохраняется как новое.

**Псевдокод:**
```python
from difflib import SequenceMatcher

def is_duplicate(new_text, old_text):
  ratio = SequenceMatcher(None, new_text, old_text).ratio()
  return ratio >= 0.95

def should_save(new_msg, old_msgs):
  for old in old_msgs:
    if new_msg['user_id'] == old['user_id']:
      if is_duplicate(new_msg['text'], old['text']):
        # Если цена изменилась — новое объявление
        if extract_price(new_msg['text']) != extract_price(old['text']):
          return True
        return False
  return True
```

**Фильтрация по ключевым словам и уведомления:**
- Все сообщения анализируются на наличие ключевых слов.
- Если ключевое слово найдено — сообщение отправляется на Telegram-аккаунт оператора с указанием времени, чата, текста и ключевого слова.

**Псевдокод:**
```python
def notify_if_keyword(message, keywords, my_telegram_id):
  for kw in keywords:
    if kw in message['text'].lower():
      send_telegram_message(
        to=my_telegram_id,
        text=f"[{message['date']}] {message['chat_title']}: {message['text']} (ключевое слово: {kw})"
      )
```

---

## 📢 СЕРВИС РАССЫЛКИ ОБЪЯВЛЕНИЙ

**Назначение:**
- Позволяет создавать объявления (текст, выбор чатов, расписание).
- Управление очередью объявлений: отправить сейчас, по расписанию, удалить, редактировать.
- Отправка объявлений во все или выбранные чаты, где работает парсер.
- Панель управления для просмотра, удаления, редактирования объявлений.

**Функционал:**
- Веб-интерфейс для создания и управления объявлениями.
- Возможность мгновенной отправки или по расписанию.
- Выбор чатов для рассылки (все или выборочно).
- История и статус отправки объявлений.

**Псевдокод:**
```python
def send_announcement(text, chat_ids, schedule=None):
  if schedule:
    # Добавить в очередь на отправку по расписанию
    schedule_announcement(text, chat_ids, schedule)
  else:
    for chat_id in chat_ids:
      send_message_to_chat(chat_id, text)
```

---

---

## 🛠️ ТЕХНОЛОГИЧЕСКИЙ СТЕК

| Компонент         | Технологии/Сервисы                |
|-------------------|------------------------------------|
| Backend           | Node.js (Express)                  |
| Парсер Telegram   | Python (Telethon, отдельный сервис)|
| Парсер WhatsApp   | Node.js (whatsapp-web.js)          |
| База данных       | Supabase (PostgreSQL)              |
| ИИ-обработка      | DeepSeek, Hugging Face, Gemini, GPT|
| Фронтенд          | React + Vite, Tailwind CSS, Chart.js|
| Хостинг           | Railway (бэкенд/парсеры), Vercel   |

---

## 💰 ТЕКУЩИЙ БЮДЖЕТ И РАСХОДЫ

| Компонент         | Сервис      | Статус      | Стоимость   | Лимиты                  |
|-------------------|-------------|-------------|-------------|-------------------------|
| ✅ Backend        | Render.com  | Работает    | $0          | Free tier               |
| ✅ Frontend       | Vercel      | Работает    | $0          | Бесплатный хостинг      |
| ✅ Telegram Parser| Railway     | Работает    | $5 (кредиты)| 1 сервис 24/7           |
| ✅ База данных    | Supabase    | Работает    | $0          | 500MB, 50K строк/мес    |
| ⏳ ИИ обработка   | Multi-AI    | Планируется | ~$2-5       | Комбинирование лимитов  |

**Текущая стоимость:** $5/мес
**Ожидаемая после ИИ:** ~$10-15/мес

---

## 📊 ТЕКУЩАЯ СТАТИСТИКА СИСТЕМЫ

### ✅ Рабочие компоненты:
- **Telegram чатов:** 7 активных
- **Ключевых слов:** 3 ('17м', 'тандем', 'груз')
- **Парсер статус:** Работает 24/7
- **База данных:** Подключена и активна
- **Веб-интерфейс:** Доступен и обновляется

### 📈 Производительность:
- **Мониторинг:** В реальном времени
- **Обновление данных:** Каждые 30 секунд
- **Uptime:** >99% (Railway, Vercel, Render)

---

## 🚀 ЭТАПЫ РАЗРАБОТКИ (ОБНОВЛЕНО)

### ✅ ЗАВЕРШЕНО (3 НЕДЕЛИ):
**Неделя 1: Базовая инфраструктура**
- ✅ Регистрация на Supabase, Railway, Vercel, Render.com
- ✅ Настройка базы данных (таблицы messages, keywords, monitored_chats)
- ✅ Парсер Telegram (7 чатов, 3 ключевых слова)

**Неделя 2: Backend и Frontend**
- ✅ Backend API на Render.com (Node.js/Express)
- ✅ Frontend на Vercel (React + Vite)
- ✅ Интеграция с Supabase
- ✅ Базовый веб-интерфейс

**Неделя 3: Развертывание и стабилизация**
- ✅ Развертывание Telegram парсера на Railway
- ✅ Успешная авторизация и работа в облаке
- ✅ Мониторинг в реальном времени
- ✅ Компонент управления ключевыми словами

### 🔨 В РАБОТЕ (ТЕКУЩАЯ НЕДЕЛЯ):
**Приоритет 1: Улучшение интерфейса**
- ✅ Создан KeywordsManager.jsx компонент
- ✅ Интегрирован в основной интерфейс
- ⏳ Тестирование добавления/удаления ключевых слов
- ⏳ Улучшение фильтрации сообщений

**Приоритет 2: Telegram уведомления**
- ⏳ Настройка Telegram Bot API
- ⏳ Отправка найденных сообщений оператору
- ⏳ Управление получателями уведомлений

### 📋 СЛЕДУЮЩИЕ ЗАДАЧИ (1-2 НЕДЕЛИ):

**Неделя 4: ИИ обработка и структурирование**
- Интеграция DeepSeek API (бесплатно)
- Пакетная обработка (200 сообщений раз в 10 минут)
- Веб-интерфейс для структурированных данных

**Неделя 5: Авто-ответы и WhatsApp**
- Парсер WhatsApp (whatsapp-web.js)
- Система авто-ответов
- Продвинутая дедупликация по алгоритму ТЗ

**Неделя 6: Статистика и финальная интеграция**
- Дашборд статистики (Chart.js)
- Ежедневные/еженедельные отчеты
- Мобильная адаптация интерфейса
- Тестирование и оптимизация

---

## 🤖 КАСКАДНАЯ СИСТЕМА ИИ ОБРАБОТКИ

**Уровень 1: Бесплатная фильтрация (Rule-based)**
```js
const URGENT_KEYWORDS = ['срочно', 'горячий', 'нужно сейчас'];
const CHINA_KEYWORDS = ['Китай', 'Гуанчжоу', 'Шанхай', 'Шеньчжень'];
function fastFilter(message) {
  const text = message.text.toLowerCase();
  return {
    priority: URGENT_KEYWORDS.some(kw => text.includes(kw)) ? 'HIGH' : 'NORMAL',
    isChina: CHINA_KEYWORDS.some(kw => text.includes(kw)),
    shouldNotify: text.includes('срочно')
  };
}
```

**Уровень 2: Пакетная обработка (Бесплатные ИИ)**
- DeepSeek — 1000 бесплатных запросов/день
- Hugging Face — 10000 бесплатных запросов/мес
- Google Gemini — 1500 бесплатных запросов/мес
- Cohere — 5 бесплатных запросов/мин

**Уровень 3: Сложные задачи (Платные ИИ)**
- GPT-3.5-Turbo — $0.0005/1K tokens (резерв)
- GPT-4 — для особо сложных задач

---

## 🔄 ДЕДУПЛИКАЦИЯ И ПРИОРИТЕТ ПО ЦЕНЕ
```js
function createCargoHash(text) {
  // Хеш без цифр (исключаем цены)
  return text.replace(/\d+/g, '').hashCode();
}
function updatePrice(existing, newMessage) {
  const newPrice = extractPrice(newMessage.text);
  return newPrice > existing.price ? newPrice : existing.price;
}
```

---

## 📊 РАСЧЕТ НАГРУЗКИ НА ИИ

| Задача              | Сообщений | Запросов ИИ | Стоимость   |
|---------------------|-----------|-------------|-------------|
| Пакетная обработка  | 6000      | 144         | $0 (бесплатные ИИ) |
| Авто-диалоги        | 150       | 10          | ~$0.15      |
| Пользовательские    | 20        | 20          | ~$0.10      |
| **Итого в день**    | 6170      | 174         | ~$0.25      |
| **Итого в месяц**   | 185,100   | 5,220       | ~$7.50      |

---

## 📱 ВЕБ-ИНТЕРФЕЙС (Схема)
```
Главная страница
├── 📊 Дашборд статистики
│   ├── График грузов по дням
│   ├── Топ направлений  
│   └── Общая статистика
├── 📦 Список грузов
│   ├── Фильтры: откуда, куда, тип, цена
│   ├── Сортировка по цене/дате
│   └── Кнопка "Авто-опрос"
├── 💬 Диалоги
│   ├── История переписки
│   ├── Статус переговоров
│   └── Управление авто-ответами
└── ⚙️ Настройки
    ├── Ключевые слова
    ├── Настройки ИИ
    └── Шаблоны сообщений
```

---

## ⚡ КОД ПАРСЕРА (Упрощенная версия)
```js
// parser.js - основной парсер
const { createClient } = require('@supabase/supabase-js');
const cron = require('node-cron');
class SmartParser {
  constructor() {
    this.supabase = createClient(SUPABASE_URL, SUPABASE_KEY);
    this.cargoCache = new Map();
  }
  async parseCycle() {
    console.log('🔄 Парсим чаты...');
    // Парсим Telegram и WhatsApp
    const telegramMessages = await this.parseTelegram();
    const whatsappMessages = await this.parseWhatsApp();
    const allMessages = [...telegramMessages, ...whatsappMessages];
    // Дедупликация и обновление цен
    for (const msg of allMessages) {
      await this.processMessage(msg);
    }
    // Пакетная обработка ИИ
    await this.batchProcess();
  }
  async processMessage(message) {
    const hash = this.createCargoHash(message.text);
    const existing = this.cargoCache.get(hash);
    const price = this.extractPrice(message.text);
    if (existing) {
      // Обновляем если цена выше
      if (price > existing.price) {
        this.cargoCache.set(hash, { ...existing, price, source: message.chat });
        await this.updateInDatabase(hash, price);
      }
    } else {
      // Новый груз
      this.cargoCache.set(hash, {
        text: message.text,
        price: price,
        source: message.chat,
        timestamp: Date.now()
      });
      await this.saveToDatabase(message);
    }
  }
}
// Запуск каждые 10 минут
cron.schedule('*/10 * * * *', () => {
  new SmartParser().parseCycle();
});
```

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ
**После каждой недели:**
- Неделя 1: База данных + 1 чат парсится
- Неделя 2: 20 чатов + дедупликация по цене
- Неделя 3: ИИ структурирование + веб-интерфейс
- Неделя 4: Авто-переговоры + голосовые сообщения
- Неделя 5: Полная система с аналитикой

**Конечный результат:**
- ✅ Автоматический сбор 6000+ сообщений/день
- ✅ Умная дедупликация с приоритетом по цене
- ✅ ИИ структурирование и авто-ответы
- ✅ Веб-интерфейс с мобильной версией
- ✅ Стоимость: ~$5-10/мес

---

## 🔄 АЛЬТЕРНАТИВНЫЕ ВАРИАНТЫ

**Вариант A: Все на VPS ($4-6/мес)**
- Плюсы: Полный контроль, высокая производительность
- Минусы: Сложнее настройка для новичка

**Вариант B: Render.com (бесплатно 90 дней)**
- Плюсы: Бесплатный старт, простая настройка
- Минусы: Через 90 дней нужна миграция

**Вариант C: Heroku (ограниченно бесплатно)**
- Плюсы: Очень просто для новичка
- Минусы: Ограниченные ресурсы

---

## 📞 ПОДДЕРЖКА РАЗРАБОТКИ

**Инструменты:**
- Visual Studio Code + GitHub Copilot
- ChatGPT/DeepSeek для помощи в кодинге
- Railway CLI для деплоя

**Ресурсы для обучения:**
- Документация Supabase
- Tutorials по Node.js/Express
- Документация Telegram Bot API

---

## 🚀 СТАРТ РАЗРАБОТКИ

**Первые шаги (День 1):**
- Зарегистрироваться на Supabase, Railway, Vercel
- Создать репозиторий на GitHub
- Настроить базовый проект в VS Code
- Подключить базу данных и сделать тестовую запись

---

**Этот документ является основным техническим заданием. Все последующие задачи и обсуждения должны опираться на него.**