[phases.setup]
nixPkgs = ['python310']

[phases.install]
cmds = ['cd telegram-parser && pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build completed for telegram-parser"']

[start]
cmd = 'cd telegram-parser && python telegram_parser.py'